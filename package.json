{
  "name": "ai-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && node proxy.js",
    "postinstall": "cd api && npm install"
  },
  "engines": {
    "node": ">=14"
  }
}
